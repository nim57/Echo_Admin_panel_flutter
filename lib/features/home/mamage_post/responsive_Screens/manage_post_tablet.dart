import 'package:flutter/material.dart';

class ManagePostTablet extends StatelessWidget {
  const ManagePostTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}